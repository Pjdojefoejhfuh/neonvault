services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: neonvault
    ports:
      - "5432:5432"
    volumes:
      - neonvault_pg:/var/lib/postgresql/data
volumes:
  neonvault_pg:
